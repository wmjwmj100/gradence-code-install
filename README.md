# Gradence Code Windows Installer

Install from PowerShell:

``powershell
irm https://raw.githubusercontent.com/wmjwmj100/gradence-code-install/master/install.ps1 | iex
``

The installer downloads the latest Windows x64 archive, verifies SHA256, installs wecode.exe, and adds it to the user PATH.